# Dependencies used by the production FastAPI service.
# Streamlit and its UI-only data stack intentionally live in requirements.txt
# while the legacy surface is being retired.
libsql==0.1.11; platform_system != "Windows" or python_version < "3.14"
reportlab>=4.2,<5
openpyxl>=3.1,<4
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
