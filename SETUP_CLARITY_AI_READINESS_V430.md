# v430 – Setup clarity & AI readiness

- Removes duplicate synchronized pitch-time control from competition rules; shows the earlier choice and links back to Planer & tider.
- Replaces the opaque 0–100 compactness number with four human-readable levels and explains both ends of the scale.
- Clarifies the optional team-completion expander as kit colours, team contact and related practical information.
- Replaces the raw missing-secret AI message with an administrator-facing activation explanation while keeping AI import disabled until a server-side OPENAI_API_KEY exists.
