# CupNavi v578 – Initial import carries plans & rules

- First setup photo/document import is reused on Step 4 Rules and Step 5 Plans & times.
- AI import now extracts structured rule values only when explicitly present; otherwise null.
- Rule values are never applied without an explicit organiser action.
- Venues from the first import can be applied as pitch names with one click.
- Match times from imported schedules are informational and are not misinterpreted as pitch opening hours.
- Existing schedule safety remains: changes mark unplayed scheduling for re-check; played history is never rewritten.
